# Minimal NetBeans project properties (cleaned)
application.title=Group7_Retronovaa
build.dir=build
build.classes.dir=${build.dir}/classes
dist.dir=dist
dist.jar=${dist.dir}/Group7_Retronovaa.jar
main.class=group7_retronovaa.Group7_Retronovaa
manifest.file=manifest.mf
src.dir=src
test.src.dir=test
javac.source=1.8
javac.target=1.8
javac.classpath=
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
includes=**
